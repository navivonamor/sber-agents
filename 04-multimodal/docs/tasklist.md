# План разработки персонального финансового советника

## 📊 Отчет о прогрессе

| Итерация | Функционал | Статус | Дата |
|----------|------------|--------|------|
| 1 | Обработка текстовых сообщений опенсорсной моделью через OpenRouter | ✅ Завершено | 2025-10-30 |
| 2 | Обработка изображений опенсорсной моделью через OpenRouter | ✅ Завершено | 2025-10-30 |
| 3 | Формирование отчета о балансе и списка транзакций | ✅ Завершено | 2025-01-30 |
| 4 | Обработка текстовых сообщений локальной моделью через Ollama | ✅ Завершено | 2025-10-30 |
| 5 | Обработка изображений локальной моделью через Ollama | ✅ Завершено | 2025-10-30 |
| 6 | Транскрибация голосовых сообщений через Faster-Whisper | ✅ Завершено | 2025-11-16 |

**Легенда статусов:**
- ⏳ Не начато
- 🚧 В работе
- ✅ Завершено
- ❌ Заблокировано

---

## 🚀 Итерации

### Итерация 1: Обработка текстовых сообщений опенсорсной моделью через OpenRouter

**Цель:** Реализовать извлечение транзакций из текстовых сообщений с использованием structured output через OpenRouter

- [x] Создать `src/models.py` с Pydantic моделями (Transaction, TransactionResponse, TransactionType, TransactionFrequency)
- [x] Обновить `src/config.py`: добавить SYSTEM_PROMPT_TEXT
- [x] Обновить `src/llm.py`: метод `get_transaction_response_text()` с structured output через `response_format` (JSON schema из Pydantic)
- [x] Обновить `src/handlers.py`: 
  - Обработчик текстовых сообщений с извлечением транзакций, сохранением в `transactions: dict[int, list[Transaction]]`, показом ответа + статус + баланс
  - Обновить `/start`: очистка истории и транзакций для пользователя
  - Добавить логирование ответов LLM (содержимое ответа + извлеченные транзакции)
- [x] Добавить зависимость `pydantic` в `pyproject.toml`

**Как протестировать:**
- Отправить `/start` → проверить очистку транзакций
- Отправить текстовое сообщение: "Сегодня купил продукты на 1500 рублей"
- Проверить извлечение транзакции (дата, сумма, категория)
- Проверить сохранение транзакции в памяти
- Проверить отображение ответа LLM + статус транзакций + баланс
- Проверить логирование ответа LLM в консоли

---

### Итерация 2: Обработка изображений опенсорсной моделью через OpenRouter

**Цель:** Добавить обработку изображений (чеки, скриншоты) с использованием VLM через OpenRouter

- [x] Обновить `src/config.py`: добавить SYSTEM_PROMPT_IMAGE (загрузка из файла)
- [x] Обновить `src/llm.py`: метод `get_transaction_response_image()` с structured output и vision API (base64 изображения)
- [x] Добавить обработчик изображений в `src/handlers.py`: получение изображения из Telegram (`message.photo` или `message.document`), конвертация в base64, передача в LLM
- [x] Те же механизмы сохранения транзакций и отображения ответа, что и для текста
- [x] Добавить логирование ответов LLM для изображений (содержимое ответа + извлеченные транзакции)

**Как протестировать:**
- Отправить изображение чека в Telegram
- Проверить распознавание текста на изображении
- Проверить извлечение транзакций из чека
- Проверить сохранение транзакций и отображение ответа + статус + баланс
- Проверить логирование ответа LLM в консоли

---

### Итерация 3: Формирование отчета о балансе и списка транзакций

**Цель:** Реализовать команды для получения отчета о балансе, статистике и списка всех транзакций

- [x] Добавить обработчик команды `/balance` в `src/handlers.py`
- [x] Расчет баланса (доходы - расходы), общих доходов, общих расходов из `transactions[chat_id]`
- [x] Статистика по категориям за все время (группировка по категориям, сумма по каждой)
- [x] Форматирование отчета и отправка пользователю
- [x] Добавить обработчик команды `/transactions` в `src/handlers.py`
- [x] Отображение всех транзакций пользователя с датой, типом, суммой, категорией
- [x] Сортировка транзакций по дате (от новых к старым)
- [x] Разбиение на несколько сообщений при превышении лимита Telegram

**Как протестировать:**
- Добавить несколько транзакций (доходы и расходы)
- Отправить команду `/balance`
- Проверить корректность расчета баланса, доходов, расходов
- Проверить статистику по категориям
- Отправить команду `/transactions`
- Проверить отображение списка всех транзакций с правильной сортировкой

---

### Итерация 4: Обработка текстовых сообщений локальной моделью через Ollama

**Цель:** Протестировать работу с локальными моделями через Ollama

- [x] Установить Ollama и запустить локальную модель
- [x] Изменить в `.env`:
  - `OPENAI_BASE_URL=http://localhost:11434/v1`
  - `MODEL_TEXT=llama3.2` (или другая модель с поддержкой structured output)
- [x] Тестирование текстовых сообщений с локальной моделью
- [x] Создать `.env.example` с примерами для Ollama
- [x] Обновить `README.md` с инструкциями по настройке Ollama

**Как протестировать:**
- Установить Ollama и запустить локальную модель
- Изменить `OPENAI_BASE_URL` и `MODEL` в `.env` на значения для Ollama
- Перезапустить бота
- Отправить текстовое сообщение с транзакцией
- Проверить работу с локальной моделью

---

### Итерация 5: Обработка изображений локальной моделью через Ollama

**Цель:** Протестировать работу обработки изображений с локальными моделями Ollama

- [x] Убедиться, что локальная модель поддерживает vision (например, llama3.2-vision)
- [x] Тестирование обработки изображений с локальной моделью

**Как протестировать:**
- Убедиться, что локальная модель поддерживает vision (например, llama3.2-vision)
- Установить соответствующую модель в `.env` (например, `MODEL=llama3.2-vision`)
- Отправить изображение чека
- Проверить работу VLM с локальной моделью

---

### Итерация 6: Транскрибация голосовых сообщений через Faster-Whisper

**Цель:** Добавить поддержку голосовых сообщений с транскрибацией через Faster-Whisper на GPU RTX 4090

- [x] Добавить зависимость `faster-whisper` в `pyproject.toml`
- [x] Создать `src/whisper.py`:
  - Метод `transcribe_audio(audio_bytes: bytes) -> str` для транскрибации аудио в текст
  - Инициализация модели Faster-Whisper при старте (singleton pattern)
  - Использование конфигурации из `config.py` (WHISPER_MODEL, WHISPER_DEVICE, WHISPER_COMPUTE_TYPE)
  - Поддержка русского языка с автоматическим определением
- [x] Обновить `src/config.py`:
  - Добавить `WHISPER_MODEL` (по умолчанию "small")
  - Добавить `WHISPER_DEVICE` (по умолчанию "cuda")
  - Добавить `WHISPER_COMPUTE_TYPE` (по умолчанию "float16")
- [x] Добавить обработчик голосовых сообщений в `src/handlers.py`:
  - Фильтр для `message.voice` или `message.audio`
  - Скачивание аудио файла через Telegram Bot API
  - Вызов `transcribe_audio()` для получения текста
  - Обработка полученного текста как обычного текстового сообщения (через существующий pipeline LLM)
  - Отображение транскрибированного текста пользователю (опционально)
  - Извлечение транзакций, сохранение, показ ответа + статус + баланс
- [x] Обновить `.env.example`:
  - Добавить секцию с настройками Faster-Whisper для GPU RTX 4090
  - `WHISPER_MODEL=small`
  - `WHISPER_DEVICE=cuda`
  - `WHISPER_COMPUTE_TYPE=float16`
- [x] Обновить `README.md`:
  - Добавить информацию о поддержке голосовых сообщений
  - Описать требования к GPU для Faster-Whisper
  - Добавить примеры использования голосовых сообщений

**Как протестировать:**
- Убедиться, что на сервере установлен CUDA и доступна GPU RTX 4090
- Установить зависимости (`uv sync`)
- Настроить `.env` с параметрами для GPU
- Запустить бота
- Отправить голосовое сообщение: "Купил продукты на полторы тысячи рублей"
- Проверить транскрибацию голоса в текст
- Проверить извлечение транзакции из транскрибированного текста
- Проверить сохранение транзакции и отображение ответа + статус + баланс
- Проверить скорость транскрибации (должна быть в реальном времени или быстрее)
- Проверить логирование в консоли

**Технические детали:**
- Faster-Whisper использует CTranslate2 для оптимизации на GPU
- Модель "small" обеспечивает баланс между качеством (93-95%) и скоростью
- float16 оптимален для RTX 4090 (Ampere архитектура)
- Ожидаемое ускорение: в 10-20 раз быстрее CPU
- Поддержка форматов: .ogg, .mp3 (форматы Telegram)

---

## 📝 Заметки

После завершения каждой итерации:
1. Обновить статус в таблице прогресса
2. Протестировать бота
3. Закоммитить изменения
4. Переходить к следующей итерации только после успешного теста

**Принципы:**
- Каждая итерация добавляет новый функционал
- Каждая итерация позволяет протестировать работу бота
- План лаконичный, только главное, принцип KISS

